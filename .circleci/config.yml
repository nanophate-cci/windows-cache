build_agent: << pipeline.parameters.build_agent >>

version: 2.1
jobs:
  build:
    parameters:
      version:
        type: string
      build_agent:
        type: string
    machine:
      image: windows-server-2019-vs2019:<< parameters.version >>
      resource_class: windows.medium
      shell: bash
    steps:
      - checkout
      - run: docker version
      - restore_cache:
          name: restore file
          keys:
            - hello-{{ .Environment.CIRCLE_WORKING_DIRECTORY }}
      - save_cache:
          name: save cache
          key: hello-{{ .Environment.CIRCLE_WORKING_DIRECTORY }}
          paths:
            - ~/cache/hello.txt

workflows:
  win-jobs:
    jobs:
      - build:
          matrix:
            parameters:
              version: ["2024.07.1", "current"]
              build_agent: ["1.0.327137-409072d0", "1.0.326836-175b641c"]

 
